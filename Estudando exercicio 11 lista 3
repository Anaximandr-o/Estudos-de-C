#include <stdio.h>

int main()
{
    int n, m, aux;
    int soma = 0;
    
    printf("Digite um numero n: ");
    scanf("%d", &n);
    printf("Digite um numero m: ");
    scanf("%d", &m);
    
    if(n > m){
        aux = n;
        n = m;
        m = aux;
    }
    
    for(int i = n; i <= m; i++){
        if(i % 2 != 0){
            printf("%d\n", i);
        }
    }

    return 0;
}
