#include <stdio.h>

int main()
{
    int m, n, i, j;
    
    printf("N e M: ");
    scanf("%d %d", &n, &m);
    
    for (int j = 1; j <= n; j++) {
    
    for (int i = 1; i <= m; i++)
    //printf ("%d", m);
    putchar ('*');
    
    putchar ('\n');

        
    }
}

