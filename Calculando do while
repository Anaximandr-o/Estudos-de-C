#include <stdio.h>

int main()
{
    float n, s;
    
    printf("Digite um valor n: ");
    scanf("%f", &n);
    
    s = n;
    do{
        s = s / 2;
        if(s > 0.01)
        printf("\n%f", s);
    }while (s > 0.01);
    
    return 0;
}
